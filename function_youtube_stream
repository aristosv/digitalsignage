#!/bin/bash

service_youtube ()
{
sudo bash -c 'cat > /etc/systemd/system/youtube.service' << 'EOL'
[Unit]
Description=youtube service
After=network.target

[Service]
ExecStart=/bin/bash -c "xinit /usr/bin/cvlc --network-caching=2000 --no-qt-privacy-ask --fullscreen --no-embedded-video --no-autoscale --width=wsize --height=hsize --loop youtubeurl"
Restart=always

[Install]
WantedBy=multi-user.target
EOL
}

service_youtube
